org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Built-in Kotlin is an AGP 9 feature and must stay off. Flutter 3.44's Gradle
# plugin force-applies `kotlin-android` to plugin subprojects regardless of this
# flag, which AGP 9 rejects when built-in Kotlin is enabled — so this project
# pins AGP to 8.x (see settings.gradle.kts) and lets Flutter supply KGP.
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
